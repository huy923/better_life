<?xml version="1.0" encoding="utf-8"?>
<androidx.wear.widget.BoxInsetLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        app:boxedEdges="all"
        android:padding="16dp">

        <!-- BPM large display -->
        <TextView android:id="@+id/tvWearBpm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--"
            android:textSize="52sp"
            android:textStyle="bold"
            android:textColor="#1D9E75"/>

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="BPM" android:textSize="14sp" android:textColor="#888888"
            android:layout_marginBottom="8dp"/>

        <!-- Animated progress bar (heart beat indicator) -->
        <ProgressBar android:id="@+id/pbHeartAnim"
            android:layout_width="48dp" android:layout_height="8dp"
            style="?android:attr/progressBarStyleHorizontal"
            android:visibility="invisible"
            android:layout_marginBottom="8dp"/>

        <!-- Status -->
        <TextView android:id="@+id/tvWearStatus"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="Chờ" android:textSize="12sp" android:textColor="#888888"
            android:layout_marginBottom="12dp"/>

        <!-- Toggle button -->
        <androidx.wear.widget.WearableButton
            android:id="@+id/btnWearToggle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bắt đầu"
            android:textSize="12sp"/>

    </LinearLayout>
</androidx.wear.widget.BoxInsetLayout>
